# Versioning and Compatibility

v0.x contracts are placeholder/reference-ready only.

Canonical contracts will later move to ForPrint Library.

Operational Registry should avoid breaking DTO names without a changelog.

Examples should include:

```text
schema_version
contract_id
contract_version
producer
consumer
fixture_status

Placeholder contracts remain non-canonical.

Runtime transport remains future Gateway responsibility.