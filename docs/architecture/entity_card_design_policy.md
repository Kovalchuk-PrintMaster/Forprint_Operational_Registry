# Entity Card Design Policy

Future entity cards should follow the data foundation strategy.

Each card should define:

```text
stable internal ID
display fields
raw imported fields
normalized fields
external references
history/version fields
terminal preview behavior
ownership boundaries

Terminal previews are allowed.

Web UI and graphical UI are not part of this step.


---
