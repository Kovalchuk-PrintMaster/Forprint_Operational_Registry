# Local Enum Drift Detection Policy

Operational Registry must detect local enum values that are not mapped to Library dictionary IDs.

Unknown local values require manual review.

Deprecated Library values remain readable but should be marked as deprecated_reference.

Alignment checks must run in check-report.