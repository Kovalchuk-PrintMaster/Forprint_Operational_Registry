# Dictionary Version Pin Policy

Operational Registry pins the Library dictionary version used for mapping.

The pin is reference-only.

It may include:

```text
library_dictionary_version
library_commit_ref
pinned_at
status
notes

No runtime Library dependency is required.