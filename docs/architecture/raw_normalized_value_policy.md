# Raw and Normalized Value Policy

For imported data, always preserve:

```text
raw_value
normalized_value
source_system
source_ref

Never destroy raw imported text.

Normalized values are helpers for lookup, reporting and validation.

Raw values preserve historical and import context.